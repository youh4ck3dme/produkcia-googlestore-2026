{
  "name": "bizagent-vercel",
  "version": "1.0.0",
  "description": "BizAgent - AI Business Assistant for Vercel",
  "scripts": {
    "build": "flutter pub get && flutter build web --release --web-renderer canvaskit --dart-define=FLUTTER_WEB_USE_SKIA=true",
    "dev": "flutter run -d chrome --web-port=5050",
    "functions:build": "cd functions && npm install && npm run build"
  },
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {},
  "keywords": [
    "flutter",
    "web",
    "bizagent",
    "accounting"
  ]
}
