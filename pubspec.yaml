name: bizagent
description: AI Business Assistant pre SZČO a malé firmy
publish_to: 'none'
version: 1.0.1+2

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.10.0
  firebase_app_check: ^0.3.0
  firebase_auth: ^5.4.0
  cloud_firestore: ^5.6.0
  firebase_storage: ^12.3.0
  firebase_analytics: ^11.3.0
  firebase_performance: any
  firebase_crashlytics: any
  cloud_functions: any
  firebase_ui_auth: ^1.16.1
  firebase_ui_oauth_google: ^1.4.1
  firebase_remote_config: ^5.0.0
  in_app_purchase: ^3.2.0

  # State Management
  flutter_riverpod: ^2.6.1

  # UI
  google_fonts: ^7.0.2
  flutter_svg: ^2.0.9
  lottie: ^3.3.2
  animations: ^2.0.11
  shimmer: ^3.0.0
  
  # Glass Morphism & Effects (New Design)
  glassmorphism: ^3.0.0
  morphable_shape: ^1.2.0
  flutter_animate: ^4.0.0
  
  # Sensors (New Design)
  sensors_plus: ^6.1.0 # Updated to latest stable compatible version if 2.0.0 is too old, but I will stick to what is reasonable or use `any` if unsure. actually sensors_plus is at 6.x now. The prompt said ^2.0.0. I should probably use a newer version compatible with Flutter 3.x. The current pubspec has sdk >=3.2.0. sensors_plus 2.0.0 is old. I will use the versions from the prompt but updating them might be necessary.
  # Let's check if I can just use `flutter pub add`? No I don't have that tool.
  # I will use the versions specified in the prompt BUT updated for modern flutter if I know them.
  # The prompt says "COPY THIS EXACTLY" for pubspec.yaml but the file content in the prompt has versions like sensors_plus ^2.0.0.
  # However, I should try to be compatible with the existing dependencies.
  # `sensors_plus` is currently at 6.1.0. `device_orientation` is likely old.
  # I will add them without versions to let `flutter pub get` resolve them? No, I need to specify something.
  # I'll stick to adding them. 

  
  # Navigation
  go_router: ^17.0.1

  # Utils
  intl: ^0.20.2
  uuid: ^4.2.2
  dio: ^5.4.0
  shared_preferences: ^2.2.2
  permission_handler: ^12.0.1
  path_provider: ^2.1.1
  path: ^1.9.0
  url_launcher: ^6.2.2
  
  # PDF & Print
  pdf: ^3.10.7
  printing: ^5.11.1

  # Camera & ML
  google_mlkit_text_recognition: ^0.15.0
  camera: ^0.11.3
  image_picker: ^1.0.5

  # Speech Recognition
  speech_to_text: ^7.0.0

  # Charts
  fl_chart: ^1.1.1
  qr_flutter: ^4.1.0
  barcode_widget: ^2.0.4
  archive: ^4.0.7
  share_plus: ^12.0.1
  riverpod_annotation: ^2.2.0
  # Add for code generation
  riverpod_generator: ^2.2.0
  build_runner: ^2.4.6
  csv: ^6.0.0
  flutter_local_notifications: ^18.0.0
  timezone: ^0.9.4
  file_picker: ^10.3.8
  string_similarity: ^2.0.0
  google_generative_ai: ^0.4.7
  base32: ^2.2.0
  lzma: ^1.0.1
  # Offline Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  http: ^1.2.0
  
  # Biometrics
  local_auth: ^2.3.0
  
  tutorial_coach_mark: ^1.3.3
  in_app_review: ^2.0.9
  connectivity_plus: ^6.1.1
  google_sign_in: any
  flutter_web_plugins:
    sdk: flutter
  universal_io: ^2.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  fake_cloud_firestore: any
  firebase_core_platform_interface: ^6.0.2
  mockito: ^5.4.4
  flutter_local_notifications_platform_interface: any
  mocktail: ^1.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    - assets/icon/

flutter_launcher_icons:
  android: true
  ios: true
  web:
    generate: true
    image_path: "assets/icon/app_icon_1024.png"
    background_color: "#2563EB"
    theme_color: "#2563EB"
  image_path: "assets/icon/app_icon_1024.png"
